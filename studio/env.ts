// The Studio is built standalone (`sanity deploy`), so it never sees the
// Next.js app's NEXT_PUBLIC_* env vars - hardcode the real project values
// here instead of depending on env vars that won't be set at build time.
export const apiVersion = "2026-01-01";

export const dataset = "production";

export const projectId = "30xk53w3";
